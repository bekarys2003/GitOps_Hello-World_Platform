image:
  repository: https://github.com/bekarys2003/GitOps_Hello-World_Platform.git
  tag: "latest" # CI will bump per env to :<git-sha>
  pullPolicy: IfNotPresent
replicaCount: 2
service:
  type: ClusterIP
  port: 80
containerPort: 3000
ingress:
  enabled: true
  className: "traefik"
  hosts:
    - host: hello.localtest.me # resolves to 127.0.0.1
      paths:
        - path: /
          pathType: Prefix